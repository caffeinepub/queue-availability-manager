// ProfileSetup is no longer used — registration now collects name during sign-up.
// This file is kept as a stub to avoid import errors in any legacy references.

interface ProfileSetupProps {
  open: boolean;
}

export default function ProfileSetup(_props: ProfileSetupProps) {
  return null;
}
